services:
  backend:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      DATABASE_PATH: /app/data/messenger.sqlite
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./data:/app/data
